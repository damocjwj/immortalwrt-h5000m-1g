* -text
# Unified patch context has an intentional leading space before source tabs.
*.patch whitespace=-blank-at-eol,-space-before-tab,-blank-at-eof
# Preserve the byte-exact pinned upstream theme, including its whitespace.
package/luci-theme-argon/** -whitespace
